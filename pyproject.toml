[project]
name = "openroute"
version = "0.1.0"
description = "OpenRoute is a simple cli for convenient interaction with popular services. "
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "gpytranslate>=2.1.0",
]
